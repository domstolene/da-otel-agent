serviceName: da-otel-agent-service
sampler: parentbased_always_on
readOnly: false
rules:
  - exclude:
    - http.target: "/metrics"
      http.method: "GET"
  - exclude:
    - http.target: "/metrics/.+"
      http.method: "POST"
